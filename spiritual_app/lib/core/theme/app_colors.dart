import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color ivory = Color(0xFFF7F3E9);
  static const Color warmPaper = Color(0xFFF1EBDC);

  // Typography
  static const Color textPrimary = Color(0xFF2C2C2C);
  static const Color textSecondary = Color(0xFF6E6A63);

  // Accents
  static const Color templeRed = Color(0xFF9F2F2F);
  static const Color gold = Color(0xFFC89B3C);

  // Utility
  static const Color white = Colors.white;
  static const Color transparent = Colors.transparent;
}
